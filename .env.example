# Frontend (React/Vite)
# API base URL for portfolio data (optional, defaults to localhost:4000 in dev)
VITE_API_URL=

# Server (Express) - kept for reference if self-hosting backend
PORT=4000
JWT_SECRET=replace_this_with_a_secure_value
ADMIN_USER=admin
ADMIN_PASS=changeme

# Postgres (used by docker-compose, optional)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=portfolio
